import { DataStringStringNumber } from './data-string-string-number';

describe('DataStringStringNumber', () => {
  it('should create an instance', () => {
    expect(new DataStringStringNumber()).toBeTruthy();
  });
});
