import { ShopValidators } from './luv2code-shop-validators';

describe('Luv2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new ShopValidators()).toBeTruthy();
  });
});
